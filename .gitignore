# Byte-compiled / cache
__pycache__/
*.py[cod]
*$py.class

# Virtual environments
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/

# Environment / secrets
.env

# OS
.DS_Store
Thumbs.db

# Data: prices_ibex_sp500.csv IS committed deliberately -- it's the fixed
# data snapshot the whole project's reproducibility depends on (see
# src/utils.py's download_data). Only other/ad-hoc data artifacts are ignored.
data/*.parquet

# IDE
.vscode/
.idea/
